### * This repo will break, change, and is only for experimentation *

### Current steps
Cd into the src directory

`$ cd OSX-OpenGL-Base/src`

then just run `Make <your file name>`

`$ make main`

build main.cpp

then just run as usual

`$ ./main`